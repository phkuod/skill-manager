# Archived Documents

These describe the original Node/Express + React/Vite stack. The running app
is now Django 5.x with a static frontend (vanilla JS + vendored Tailwind).
See `CLAUDE.md` at the repo root for the current architecture.

- `plan.md` — original implementation plan
- `spec.md` — original product spec
