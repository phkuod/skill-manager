# Shared defaults — copy to .env and adjust as needed.
# For environment-specific overrides, use .env.development or .env.production

# Server port
PORT=3000

# API port (development only — Vite proxies requests to this port)
API_PORT=3001

# Path to the skill repository directory
SKILL_REPO_PATH=./skill_repo
